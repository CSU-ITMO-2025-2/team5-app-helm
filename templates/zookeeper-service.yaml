apiVersion: v1
kind: Service
metadata:
  name: {{ include "team5-app.fullname" . }}-zookeeper-svc # Имя сервиса: <имя-релиза>-zookeeper-svc
  labels:
    {{- include "team5-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: zookeeper
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.zookeeper.service.port }}
      targetPort: {{ .Values.zookeeper.service.port }}
      protocol: TCP
      name: client
  selector:
    app.kubernetes.io/name: {{ include "team5-app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: zookeeper
    